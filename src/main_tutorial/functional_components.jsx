export const FunctionalComponents = () => {
  return (
    <div>
      <h1>Functional Components</h1>
      {/* Что такое функциональный компонент?
        Функциональный компонент — это один из способов создания компонентов в React.
        Это обычная JavaScript-функция, которая:
        1. Принимает props (входные параметры)
        2. Возвращает JSX (разметку) */}
    </div>
  );
}